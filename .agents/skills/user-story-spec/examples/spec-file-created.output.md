# Spec File Created Expected Output

```text
Spec created at `docs/save-product-favorites.md`.
```

Expected rules:

- The `docs/` folder exists or was created.
- The file was created inside `docs/`.
- The filename is English, lowercase, and kebab-case.
- The filename is related to the objective or behavior of the spec.
- No spec file was created in the project root.
